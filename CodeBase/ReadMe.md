Final code files for submission

`utils.py` includes helper function for unsupervised anomaly detection, `sequential_forward_selection.ipynb`  includes the code and performance of multiple unsupervised anomaly detection on valid data. 